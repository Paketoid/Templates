#include <bits/stdc++.h>

using namespace std;

ostream& operator << (ostream& out, const vector <auto> v){
    for (auto x : v){
        out << x << ' ';
    }

    return out;
}


int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);


return 0;
}
